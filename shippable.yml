# Language setting
language: node_js

#build_image: drydock/u12nod:prod

# Version number
node_js:
  - 0.12
  
build:
  
  ci:
    - ps -eax
    - ls -al
    - echo $M1

  post_ci:
    - docker build -t=quay.io/qhstest01/server-sample-node .
    - docker push quay.io/qhstest01/server-sample-node


integrations:
  hub:
    - integrationName: "ship-quay"
      type: "quay.io"
