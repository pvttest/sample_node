# Language setting
language: node_js

#build_image: drydock/u12nod:prod

# Version number
node_js:
  - 0.12
  
build:
  pre_ci:
    - docker build -t=shiphitchcock2/server_sample_node .

  pre_ci_boot:
    image_name: shiphitchcock2/server_sample_node
    image_tag: latest
    pull: false
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'

  ci:
    - ps -eax
    - ls -al
    

  #post_ci:
   # - docker push shippabledocker/shiphitchcock2_sample_node 

integrations:
  hub:
    - integrationName: ship-docker
      type: docker
